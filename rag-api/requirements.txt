# ── API Web ───────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── Configuration & Validation ────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.0.1

# ── Graphe RDF / SPARQL ───────────────────────────────────────────────────────
rdflib==7.0.0

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb==0.5.3
sentence-transformers==3.0.1

# ── LangChain (orchestration LLM) ────────────────────────────────────────────
langchain==1.3.8
langchain-core==1.4.6
langchain-community==0.4.2
langchain-text-splitters==1.1.2

# ── LLM Providers (au moins un requis) ───────────────────────────────────────
langchain-google-genai==4.2.5    # Gemini (recommandé, gratuit)
langchain-anthropic==1.4.5       # Claude
langchain-openai==1.3.0          # OpenAI GPT + DeepSeek
langchain-groq==1.1.3            # Llama via Groq (gratuit)

# ── Utilitaires ───────────────────────────────────────────────────────────────
pandas==2.2.2
tqdm==4.66.4
structlog==24.2.0
tenacity==8.4.1
httpx==0.28.1

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
